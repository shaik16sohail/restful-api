<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="style.css" class="">
</head>
<body>
    <h1>Quora Posts</h1>
    <% for(i of posts){%>
        <div class="post">
            <h3 class="user">@<%= i.username %></h3>
            <h4 class="content"><%= i.content%></h4>
            <br>
            <a href="http://localhost:3000/posts/<%= i.id %>">See in Detail</a>
            <a href="http://localhost:3000/posts/<%= i.id %>/edit">Edit</a>
            <!-- &nbsp;&nbsp; -->
            <br>
            <form action="/posts/<%= i.id%>?_method=DELETE" method="post">
                <button>Delete post</button>
            </form>
        </div>
    <% } %>
    <br><br>
    <a href="http://localhost:3000/posts/new">Create New Post</a>
</body>
</html>